{
  "name": "ckb-voting-deploy",
  "version": "1.0.0",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "deploy": "ts-node -P tsconfig.json deploy.ts",
    "seed": "ts-node -P tsconfig.json seed.ts",
    "smoke": "ts-node -P tsconfig.json smoke.ts",
    "setup:multi": "ts-node -P tsconfig.json setup-multi-actors.ts",
    "smoke:multi": "ts-node -P tsconfig.json smoke-multi.ts",
    "build-contract": "cd ../contracts-rust && cargo build --release -p governance-contract"
  },
  "dependencies": {
    "@ckb-ccc/core": "^1.18.1",
    "dotenv": "^16.6.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0",
    "ts-node": "^10.9.0"
  }
}
