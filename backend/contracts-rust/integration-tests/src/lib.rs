//! CKB-VM integration tests for the compiled governance contract binary.
//!
//! The actual VM cases live under `tests/`. Build the on-chain binary first
//! with `pnpm build:contract:rust` or `cargo build --release -p governance-contract`.
