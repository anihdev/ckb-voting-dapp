[package]
name = "governance-contract"
version = "0.1.0"
edition = "2021"

[dependencies]
ckb-hash = { workspace = true }
ckb-std = { workspace = true }
sparse-merkle-tree = { workspace = true, features = ["smtc"] }

[features]
default = []
native-simulator = []
